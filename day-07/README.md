# Day 07 - To-do List

## Bugün Öğrendiklerim

- `document.createElement()` ile dinamik HTML öğeleri oluşturma
- `appendChild()` ile öğeleri DOM'a ekleme
- `.remove()` ile öğe silme
- `<span>` ile görev metnini butonlardan ayırma
- `classList.add()`, `toggle()` ve `contains()` kullanımı
- `querySelectorAll()` ve `.length` ile öğeleri sayma
- Click ve keydown olaylarını kullanma
- JavaScript ile arayüzü güncelleme

## Uygulama Özellikleri

- Yeni görev ekleme
- Boş görevleri engelleme
- Enter tuşuyla görev ekleme
- Görevi tamamlandı olarak işaretleme
- Tamamlanan görevi geri alma
- Görev silme
- Toplam ve tamamlanan görev sayısını gösterme
- Responsive tasarım